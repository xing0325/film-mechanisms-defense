# 电影如何让世界运转

课程答辩互动网站：以《老无所依》《黄金三镖客》《爵士春秋》为三章，把信仰、类型与表演做成可操作的电影观看实验。

## 本地运行

```bash
npm install
npm run dev
```

## 操作

- 滚轮推进叙事；点击完成每章实验。
- `1` / `2` / `3`：跳转章节；`Home`：返回首页；`Space`：下一幕。
- `M`：声音开关；`R`：重置本章；`F`：全屏。

GitHub Pages 由 `.github/workflows/deploy-pages.yml` 在推送到 `main` 后自动发布。
